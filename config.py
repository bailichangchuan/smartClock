# ==============================================================================
# 项目全局配置中心
# 说明：所有模块的核心参数统一在此配置，修改时需确保与硬件/服务参数一致
# 注意：1. 字符串参数需用双引号包裹；2. 数值参数直接填写（无需引号）；3. 引脚/波特率需与外设手册匹配
# ==============================================================================

# ==================== WiFi 配置（2.4G网络，不支持5G）====================
# WiFi名称（SSID）：替换为你的路由器2.4G网络名称
WIFI_SSID = "l888"
# WiFi密码：替换为你的路由器2.4G网络密码（长度≥8位）
WIFI_PWD = "12345678"

# ==================== 知心天气 API 配置（获取实时天气数据）====================
# 天气API密钥：替换为你的知心天气开发者Key（需注册账号获取，https://www.seniverse.com/）
SENIVERSE_KEY = "xxxxxx"
# 定位方式：可选值→"ip"（自动IP定位）、具体城市名（如"北京"、"上海"，需符合API格式）
LOCATION = "ip"
# 天气API域名：知心天气官方域名，无需修改
API_DOMAIN = "api.seniverse.com"
# 天气API请求路径：拼接Key、定位方式、语言（中文）、温度单位（摄氏度），无需修改
API_PATH = f"/v3/weather/now.json?key={SENIVERSE_KEY}&location={LOCATION}&language=zh-Hans&unit=c"
# 天气数据刷新间隔：单位（秒），控制多久请求一次新天气（建议≥10秒，避免API限流）
WEATHER_REFRESH_INTERVAL = 10

# ==================== NTP 时钟配置（校准系统时间）====================
# NTP服务器地址：国内稳定服务器（阿里云），无需修改；可选替换为"ntp1.aliyun.com"等
NTP_SERVER = "ntp.aliyun.com"
# NTP同步超时时间：单位（秒），超过该时间未响应则视为校准失败
NTP_TIMEOUT = 10
# 时区偏移：单位（小时），中国时区为UTC+8，固定填写8
TIMEZONE_OFFSET = 8

# ==================== 系统调度配置（控制模块执行频率）====================
# NTP校准周期：单位（小时），控制多久重新校准一次系统时间（建议≥1小时，避免频繁网络请求）
NTP_CALIBRATION_HOURS = 1
# 串口时间输出间隔：单位（秒），控制控制台多久打印一次当前时间（仅调试用，不影响功能）
TIME_PRINT_INTERVAL = 5

# ==================== 串口1配置（传感器专用，与多传感器模块通信）====================
# 串口编号：ESP32支持的串口编号（可选0/1/2，需与硬件引脚分配一致）
UART_NUM = 1
# 波特率：传感器通信波特率（必须与传感器手册一致，默认9600）
UART_BAUDRATE = 9600
# TX引脚：ESP32的TX引脚编号（连接传感器的RX引脚，需按硬件接线修改）
UART_TX_PIN = 3
# RX引脚：ESP32的RX引脚编号（连接传感器的TX引脚，需按硬件接线修改）
UART_RX_PIN = 2

# ==================== 传感器读取配置（控制传感器数据采集频率）====================
# 传感器数据读取间隔：单位（秒），控制多久读取一次传感器数据（建议≥2秒，避免传感器拥堵）
SENSOR_READ_INTERVAL = 2

# ==================== 串口2配置（串口屏专用，与串口屏通信）====================
# 串口编号：ESP32支持的串口编号（可选0/1/2，需与硬件引脚分配一致，建议与传感器串口区分）
SERIAL_PORT = 2
# 波特率：串口屏通信波特率（必须与串口屏配置一致，默认115200）
SERIAL_BAUD_RATE = 115200
# TX引脚：ESP32的TX引脚编号（连接串口屏的RX引脚，需按硬件接线修改）
SERIAL_TX_PIN = 5
# RX引脚：ESP32的RX引脚编号（连接串口屏的TX引脚，需按硬件接线修改）
SERIAL_RX_PIN = 4